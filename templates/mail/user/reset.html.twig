{% extends 'mail.html.twig' %}

{% block body %}

    <p>Password Reset Link:</p>

    {% set url = url('auth.reset-password.confirm', {'token': token}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>

{% endblock %}
